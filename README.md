# gitlearning
for git learning

```git
git config user.name "name"
git config user.email "email"

git config user.name
git config user.email

git add 文件名
git add .
git status

git commit -m"提交说明"

git clone https/ssh
```



